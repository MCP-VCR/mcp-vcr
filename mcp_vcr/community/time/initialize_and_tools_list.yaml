meta:
  version: 1
  recorded_at: "2026-08-16T12:00:00.000Z"
  session_id: "fa110006"
  server_command: ["uvx", "--with", "mcp>=1.23,<2", "mcp-server-time"]
  protocol_version: "2024-11-05"
  client_hint: "mcp-vcr-community"
  schema_version: "1.0"

messages:
  - t: 0
    dir: c2s
    payload:
      jsonrpc: "2.0"
      id: 1
      method: initialize
      params:
        protocolVersion: "2024-11-05"
        capabilities: {}
        clientInfo:
          name: "mcp-vcr-community"
          version: "0.2.1"

  - t: 25
    dir: s2c
    payload:
      jsonrpc: "2.0"
      id: 1
      result:
        protocolVersion: "2024-11-05"
        capabilities:
          tools: {}
        serverInfo:
          name: "mcp-server-time"
          version: "0.1.0"


  - t: 30
    dir: c2s
    payload:
      jsonrpc: "2.0"
      method: notifications/initialized

  - t: 40
    dir: c2s
    payload:
      jsonrpc: "2.0"
      id: 2
      method: tools/list
      params: {}

  - t: 65
    dir: s2c
    payload:
      jsonrpc: "2.0"
      id: 2
      result:
        tools:
          - name: "get_current_time"
            description: "Get the current time in a specified timezone or UTC."
            inputSchema:
              type: object
              properties:
                timezone:
                  type: string
                  description: "IANA timezone name (e.g. 'America/New_York', 'UTC')."
              required: ["timezone"]
          - name: "convert_time"
            description: "Convert time between different timezones."
            inputSchema:
              type: object
              properties:
                source_timezone:
                  type: string
                target_timezone:
                  type: string
                time:
                  type: string
              required: ["source_timezone", "target_timezone", "time"]
