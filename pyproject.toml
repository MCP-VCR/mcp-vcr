[project]
name = "mcp-vcr"
version = "0.2.0"
description = "A deterministic, replayable, human-readable MCP transcript format and proxy."
authors = [
    { name = "mcp-vcr contributors" }
]
dependencies = [
    "pydantic>=2.0.0",
    "pyyaml>=6.0.1",
    "click>=8.1.0",
    "jsonschema>=4.20.0",
]
requires-python = ">=3.12"

[project.optional-dependencies]
sse = [
    "aiohttp>=3.9.0"
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
]

[project.scripts]
mcp-vcr = "mcp_vcr.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
asyncio_mode = "auto"
pythonpath = [
  ".",
  "plugins/pytest-mcp-vcr"
]

[tool.setuptools]
packages = ["mcp_vcr", "mcp_vcr.schemas", "mcp_vcr.transports"]

[tool.setuptools.package-data]
mcp_vcr = ["schemas/transcript-schema-v1.json"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
